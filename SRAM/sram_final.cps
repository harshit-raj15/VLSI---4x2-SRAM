                                                                               
; autoPinSwap() results for schematic network.

swapPins("precharge" "(p bl_bar bl)")

swapPins("2nand" "(p a b)")

swapPins("inverter" "")

swapPins("rowdecoder" "(p (f a0 w1) (f a1 w2))")

swapPins("sramcell" "(p bl bl_bar)")

swapPins("columndecoder" "")

swapPins("senseamp" "")

swapPins("read_write" "")
